# Copy to .env.local for local development. NEVER commit .env.local.
#
# Troubleshooting #6: NEXT_PUBLIC_* are inlined at build time.
# Changing them in Vercel requires a fresh deploy via `vercel env add` (not -e).
#
# Troubleshooting #14: NEXT_PUBLIC_API_URL must NOT have a trailing slash.
# The app strips it defensively in lib/api.ts, but keep it clean here too.

NEXT_PUBLIC_API_URL=https://6svkxadr3h.execute-api.us-east-1.amazonaws.com
NEXT_PUBLIC_SESSION_CODE=LATAM2026
