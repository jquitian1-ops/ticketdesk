# Terraform state and lock
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log

# Local tfvars (commit only .example versions)
*.tfvars
!*.tfvars.example

# Placeholder bundle is regenerated locally; keep canonical out of git
dummy.zip
